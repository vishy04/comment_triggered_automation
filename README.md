# IG DM Bot

A bot that connects to your Instagram account, watches a post for a keyword and sends a DM to the users.

## Setup

1. Copy `.env.example` to `.env`
2. Fill out the variable in the `.env` file!

To launch:
```
uv run python src/main.py
```
